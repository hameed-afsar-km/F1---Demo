I want you to implement a horizontal scroll effect in this section, where I have a set of images under the features folder, under the Assets folder. Each image has its own name as {feature_name}.png. Use this detail to display each image in a scrolling horizontal animation along with a text with respect to the theme of the website, under each image. The horizontal scroll should begin when the section is completely covered, and the section should end only when the complete horizontal scroll is finished. Also, have a fixed title of "FEATURES that DISTINGUISH US" in the section header.