I want a section to show the people who come under the Ferrari racing f1 team. I have a set of people with their name as {racer_name.png} in the drag-scroll folder under Assets folder. The ratio of each card is 4:5 with padded round borders. Do not change the ratio. Have proper cursor hover animation on each image. Each image should show their respect name, when the cursor is hovered over the image. That is, a cursor reveal. and it only reveals on hovering, else returns to normal custom cursor. The cursor has morphing animation to name revealing and custom cursor transition.
I want you to arrange them and create the following design:


Objective: Build a high-performance, cinematic 3D carousel using React, GSAP (with Draggable), and Vanilla CSS. The carousel should feature a series of "Driver Cards" arranged in a 3D elliptical orbit that users can drag to rotate infinitely.

1. Structural Architecture
3D Scene: Create a container with perspective: 2000px to host the carousel.
The Ring: A central track div with transform-style: preserve-3d to maintain the 3D space for its children.
The Cards: Absolute-positioned divs representing cards (suggested size: 420px x 525px).
2. Mathematical Orbit Logic
Arrange cards along an ellipse in 3D space using Math.sin() for X and Math.cos() for Z.
Radius: Use a wide horizontal radius (e.g., 500px) and a shallower depth radius (e.g., 300px).
Dynamic Visuals based on Depth (Z-axis):
Scale: Cards in front should be 1.0 scale; cards in back should shrink to 0.6.
Opacity: Cards in front 1.0 opacity; cards in back fade to 0.2.
Z-Index: Dynamically set zIndex based on the Z-position to ensure correct layering.
Rotation: Apply a slight rotationY tilt to cards based on their X-position to give a "curved" feel as they move.
3. Interaction & Motion (GSAP)
Infinite Draggable: Use GSAP Draggable on an invisible proxy element to capture horizontal drag.
Progress Tracking: Map the drag distance to a progress variable. 1 unit of progress should represent one full card shift.
Inertia & Snapping:
Enable inertia: true so the carousel keeps spinning after a flick.
Implement a snap function that ensures the carousel always settles with a card perfectly centered (round the progress to the nearest integer).
Fluidity: Use gsap.set() or gsap.to() for high-performance updates on every frame during drag/throw.
4. Visual Aesthetics & UI
Background: A pure black (#000) section with a large, subtle radial gradient glow (rgba(220,0,0,0.1)) centered behind the carousel.
Card Design:
Rounded corners (16px) and a thin border (1px solid rgba(255,255,255,0.1)).
A deep bottom-shadow for depth.
A bottom-up black gradient overlay to make the text readable.
Typography:
Use Bebas Neue for large, aggressive titles and driver names.
Use Rajdhani for technical details (Team Name, Section numbers).
Large Background Numbers: Place a large, semi-transparent number (e.g., rgba(220,0,0,0.15)) behind the driver's name inside the card.
5. Entrance Animation
On mount, the cards should stagger into view from the bottom (y: 200) with opacity: 0 using a power3.out easing for a smooth, high-end reveal.
Tech Stack Requirements:

framer-motion (for basic layout animations)
gsap & gsap/Draggable (for the 3D physics)
next/image (if using Next.js) or standard <img> tags.